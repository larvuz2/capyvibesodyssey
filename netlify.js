// This file is used by Netlify to ensure proper client-side routing
// It's not used at runtime, just during the build process

console.log('Netlify build process detected. Configuring client-only setup...');
console.log('WASM binary support enabled via Vite plugins.');
console.log('Client-side redirects configured for SPA routing.');

// This is just a helper file, not actually used in the build process
// The real configuration happens in netlify.toml